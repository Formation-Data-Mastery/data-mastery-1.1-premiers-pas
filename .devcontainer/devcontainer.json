{
  "name": "Data Mastery — Module 1",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "remoteUser": "apprenant",
  "postCreateCommand": "curl -LsSf https://astral.sh/uv/install.sh | sh && ~/.cargo/bin/uv sync && ~/.cargo/bin/uv run python -m ipykernel install --user --name=data-mastery --display-name='Data Mastery (Python 3.12)' && npm install -g @anthropic-ai/claude-code",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter",
        "ms-toolsai.vscode-jupyter-slideshow",
        "ms-toolsai.vscode-jupyter-cell-tags"
      ],
      "settings": {
        "python.defaultInterpreterPath": "/home/apprenant/.venv/bin/python",
        "jupyter.kernels.filter": [],
        "python.terminal.activateEnvironment": true
      }
    }
  },
  "forwardPorts": [8888, 8501]
}
